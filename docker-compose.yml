version: "3.8"
services:
  mysqldb:
    image: mysql:5.7
    restart: unless-stopped
    environment:
      #- MYSQL_ROOT_PASSWORD=
      - ALLOW_EMPTY_PASSWORD=yes
      - MYSQL_DATABASE=stationSki
    ports:
      - 3307:3307
    volumes:
      - db:/var/lib/mysql
  gestion-station-ski:
    depends_on:
      - mysqldb
    image: beanecib/gestion-station-ski:1.0
    restart: on-failure
    ports:
      - 8089:8089
    environment:
      SPRING_APPLICATION_JSON: '{
      "spring.datasource.url=jdbc" : "mysql://mysqldb:3307/stationSki?createDatabaseIfNotExist=true",
      "spring.datasource.username" : "root",
      "spring.datasource.password" : "",
      "spring.jpa.show-sql" : "false",
      "spring.jpa.hibernate.ddl-auto" : "update",
      "spring.jpa.properties.hibernate.dialect" : "org.hibernate.dialect.MySQL5InnoDBDialect" 
      }'

    stdin_open: true
    tty: true
volumes:
  db: